# Agent notes — kdl-d

Project facts for agents. Workstation/env facts live only in `$CODE_ROOT/MEMORIES.md`.

- DUB package name is `kdl`; GitHub repo is `dlang-supplemental/kdl-d`
- Registry metadata assets live in `assets/` (`logo.png`, `dub-registry.yaml`)
- Categories: `library.data`, `library.development.parsing`
- Publish via sibling `dub-publish` / `dubx`
