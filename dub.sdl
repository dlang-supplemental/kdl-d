name "kdl"
description "Native KDL document language parser and writer for D (v1 + v2 hybrid)"
authors "amdphreak"
copyright "Copyright © 2026, amdphreak"
license "BSL-1.0"
# Version comes from git tags (vX.Y.Z).

targetType "library"

importPaths "source"
sourcePaths "source"
